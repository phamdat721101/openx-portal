{
  "name": "openx-deep-research-analyst-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1 skeleton for the Node-side gateway sidecar. Wraps HyperMove MCP + nim-skill + n-payment for the Python ADK agent to call over local HTTP. See ../README.md for phase status.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "4.21.2"
  },
  "devDependencies": {
    "@types/express": "4.17.21",
    "@types/node": "22.10.5",
    "tsx": "4.19.2",
    "typescript": "5.7.3"
  }
}
